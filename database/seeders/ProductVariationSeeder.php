<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class ProductVariationSeeder extends Seeder
{
    public function run(): void
    {
        // Don't create any variations in the seeder anymore
        // Variations will be created through the admin panel with proper attributes
        
        // This seeder is now empty on purpose - we want a clean start
        // with only relational attribute-based variations
    }
}
